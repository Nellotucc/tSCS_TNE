% Sample function getDataFromChannel
function data = getDataFromChannel_test(channel, sampleRate, numberOfValuesmax)
    % Generate sample data (random numbers)
    data = rand(1, numberOfValuesmax);
end

